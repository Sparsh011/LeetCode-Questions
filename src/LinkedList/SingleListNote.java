package LinkedList;

public class SingleListNote {
      int val;
      SingleListNote next;
      SingleListNote() {}
      SingleListNote(int val) { this.val = val; }
      SingleListNote(int val, SingleListNote next) { this.val = val; this.next = next; }
  }
